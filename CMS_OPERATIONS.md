# HP運用と公開設定

## メニューを改訂する

1. Notionの「HP運営ポータル → メニュー → メニュー公開設定」を開く。
2. 対象店舗を「投稿しない」にする。
3. 日本語・英語のメニューページを再読み込みし、準備中になったことを確認してから編集する。
4. 品名・価格・写真を入力し、「カテゴリー」「アクセント色」を選択する。
5. 内容確定後、店舗の設定を「投稿する」にする。「表示する」が付いた商品だけ掲載される。
6. 日英・PC・スマホを確認する。商品や通常の文章はキャッシュ更新に約1〜2分以上かかる場合がある。

公開設定DBは店舗ごとに1行だけ。未入力・重複・取得失敗は非公開として扱う。
公開判定は毎リクエスト取得し、メニューページは動的生成・no-store。
非公開時に仮メニューへ戻す処理はなく、商品データもページへ渡さない。
すでに開いている画面・過去の配信先の情報を消す機能ではない。

## 選択式の入力

- カテゴリーは「日本語｜English」の共通選択肢。日英商品行で同じものを選ぶ。
- アクセント色：抹茶／深緑／ほうじ茶／金茶／古金／石色／墨／白茶。商品名横の細い線に反映。
- 写真は中央に自動配置。上下・左右・拡大率と調整用の選択欄は廃止。写真を差し替えるだけでよい。
- 英語写真が空なら「対応する日本語商品」から写真を引き継ぐ。
- 旧カテゴリ列・旧色コード列は旧本番との互換用。通常ビューから隠し、日常入力では使わない。

## 日英の予約URL

「予約・検索・表示」で店舗の「予約ページ」「予約ページ（英語）」を編集する。
英語が空欄なら日本語URLへ。HTTPSのURLのみ利用できる。
外部予約サイトを使う場合は「受付方法」も変更する。「受付停止」は受付導線を隠す。
予約者・空席・決済・通知は予約サービスで管理し、公開CMSには保存しない。

## デプロイと互換性

Notion編集だけならGit操作もNotionのWeb公開も不要。
コード変更はredesignで検査してプレビューへ。mainへの切替は別途承認する。
旧mainは同じ元DBを使用するが、新しい公開スイッチを解釈しない。
旧DBの削除や共有項目の編集前には、本番での使用状況を確認する。

## 検査

```sh
npm run lint
node --test scripts/check-story.mjs scripts/check-editing.mjs scripts/check-menu-publishing.mjs scripts/check-cms-publishing.mjs
npm run build
node scripts/check-routes.mjs http://127.0.0.1:3100
node scripts/check-security.mjs http://127.0.0.1:3100
# 両店舗とも投稿しないのときだけ実施
node scripts/check-menu-closed.mjs http://127.0.0.1:3100
```

CMS結合テストはインメモリのテストデータのみを使う。実際の未確定メニューをテストのために公開しない。
